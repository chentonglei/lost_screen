import * as t from '@babel/types';
import { NodePath } from '@babel/traverse';
export interface IOpts {
    flag?: string;
}
export default function (): {
    visitor: {
        ImportDeclaration(path: NodePath<t.ImportDeclaration>, { opts }: {
            opts: IOpts;
        }): void;
        VariableDeclarator(path: NodePath<t.VariableDeclarator>, { opts }: {
            opts: IOpts;
        }): void;
    };
};
